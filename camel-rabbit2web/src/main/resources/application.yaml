server:
  port: 4444

spring:
  application:
    name: camel-rabbit2web
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    virtual-host: ${RABBITMQ_VHOST}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

# turn on auto declare so the exchange, queues are automatic created if not already present in rabbitmq broker
camel:
  component:
    spring-rabbitmq:
      auto-declare: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

#soap.service.base.url: /my-foo-api
#cxf.servicelist.title: Extremely customized List-Title
#soap.messages.logging: true
#soap.messages.extract: true